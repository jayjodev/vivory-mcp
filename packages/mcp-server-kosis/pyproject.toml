[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vivory-mcp-kosis"
version = "0.1.4"
description = "DEPRECATED v0.1.4 — migrate: uvx vivory-mcp-verification (Korean verdict tools included, same $29/mo Pro key)."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
    { name = "Vivory", email = "contact@vivory.app" },
]
keywords = ["mcp", "kosis", "korea", "anthropic", "claude", "deprecated", "verifiable-ai"]
classifiers = [
    "Development Status :: 7 - Inactive",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "mcp>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "jsonschema>=4.0.0",
]

[project.urls]
Homepage = "https://vivory.app"
Repository = "https://github.com/jayjodev/vivory-mcp"
Issues = "https://github.com/jayjodev/vivory-mcp/issues"
"Successor (vivory-mcp-verification)" = "https://pypi.org/project/vivory-mcp-verification/"

[project.scripts]
vivory-mcp-kosis = "vivory_mcp_kosis.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/vivory_mcp_kosis"]
